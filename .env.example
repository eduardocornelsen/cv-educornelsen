# Gemini API key — used by the chatbot backend proxy.
# Get yours at https://aistudio.google.com/app/apikey
#
# NEVER commit .env to git. In production, set this directly
# in Cloud Run environment variables or Secret Manager.
GEMINI_API_KEY=your-gemini-api-key-here
